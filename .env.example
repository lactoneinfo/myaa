DISCORD_BOT_TOKEN="replace‑with‑token"

# === LLM Provider Configuration ===

LLM_PROVIDER=gemini

# Your Gemini API key (required if LLM_PROVIDER=gemini)
GEMINI_API_KEY=your_gemini_api_key_here

# Gemini model to use:
# - gemini-pro            (default: general text model)
# - gemini-1.5-pro        (128k context, faster, more powerful)
# - gemini-pro-vision     (supports image input, not required here)
GEMINI_MODEL=gemini-2.0-flash